import styled from 'styled-components'

export const Container = styled.div`
    position: absolute;
    right: 0;
    top: 0;
    padding: 2rem 2rem 0;
    overflow: hidden;
    /* width: min(400px, 100%); */
    width: min(400px, 100%);

    z-index: 9999;
`
